---
description: Workflow de desenvolvimento — ciclo plan→execute→test, skills, comandos e teste visual com CDP
alwaysApply: true
---

# Reverso Agent — Workflow

## Ciclo de desenvolvimento

```
plan-feature → busca-componentes → storybook-variations (UI relevante) → execute-plan → validate (typecheck+tests+smoke) → commit → post-commit-tests → run-tests
```

- **plan-feature** gera plano em `.agents/plans/` (não escreve código).
- **busca-componentes** — para tarefas de UI, buscar no shadcn/ui e shadcnblocks se já existe componente/bloco pronto antes de implementar.
- **storybook-variations** — para mudanças relevantes de UI, criar e validar variações no Storybook antes de integrar no app real.
- **execute-plan** implementa tarefas do plano e valida (VALIDATE por tarefa, VALIDATION COMMANDS ao final).
- **commit** faz commit atômico com tag (feat/fix/refactor/test/docs/chore/style).
- **post-commit-tests** analisa o commit, gera/atualiza testes e registra em `.agents/test-registry.md`.
- **run-tests** roda a suíte (all ou last) conforme o registro.
- **validate** deve incluir, quando aplicável:
  - `pnpm typecheck` no escopo alterado
  - testes focados do fluxo alterado
  - smoke com prompts/comandos reais para fluxos conversacionais do agente

## Busca de componentes UI (obrigatório)

Antes de implementar qualquer componente de UI, buscar nos registries:

1. **shadcn/ui oficial** — primitivos e componentes base
2. **shadcnblocks** (Pro) — 500+ blocos prontos (shells, sidebars, forms, tables, etc.)

Usar o **shadcn MCP** no Cursor para busca por linguagem natural ou a CLI (`pnpm dlx shadcn@latest add @shadcnblocks/[nome]`).

## Storybook-first para UI (recomendado)

- Para **screens, blocos e interações relevantes**, seguir fluxo recomendado:
  1. criar variações no Storybook;
  2. validar visual e comportamento;
  3. implementar no app real após aprovação explícita quando aplicável.
- Para ajustes pequenos e pontuais, pode implementar direto no app.
- Em caso de divergência, a versão aprovada no Storybook vira referência para implementação.

## Skills e quando usar

| Skill | Uso |
|-------|-----|
| **plan-feature** | Planejar uma feature completa (análise, Context7, plano em disco). Nunca escreve código. |
| **inspiration-research** | Pesquisar inspiração nos projetos em `.agents/inspirations/` (subagentes explore + decisor). |
| **full-stack-electron** | Consultar padrões e API do Electron (main/renderer, IPC, BrowserWindow, packaging). |
| **electron** (CDP) | Validar UI por inspeção no navegador: lançar com `--remote-debugging-port`, conectar via agent-browser, snapshot e verificação de elementos/texto. |

## Comandos

- **/plan-feature [nome]** — Gera plano em `.agents/plans/{kebab-case}.md`.
- **/execute-plan [plano]** — Executa tarefas; usa `.agents/plans/` se path não for absoluto.
- **/storybook-variants [escopo]** — Cria variações no Storybook e guia seleção da versão final antes da integração.
- **/inspire-from-projects [tópico]** — Busca inspiração (usa skill inspiration-research).
- **/load-project-context** — Carrega estrutura do projeto e estado do git.
- **/commit** — Commit atômico com tag e mensagem.
- **/post-commit-tests** — Gera/atualiza testes após commit; atualiza `.agents/test-registry.md`.
- **/run-tests [all\|last]** — Roda suíte completa ou só testes do último commit no registro.

## Agent Lab (fluxo atual)

- Superfície conversacional:
  - `pnpm reverso agent --text|--prompt "..."`
  - `pnpm reverso --text|--prompt "..."`
- Fluxo investigativo principal:
  - `init -> deep-dive (alias legado: dig) -> deep-dive-next -> create-lead -> inquiry`
- Em mudanças do `lab/agent`, priorizar validação de:
  - roteamento stateful
  - continuidade de sessão
  - comportamento em linguagem natural para plan/execute

## Teste visual com Electron (CDP)

Após implementar UI ou fluxos visuais, usar a skill **electron** (`.cursor/skills/electron/SKILL.md`):

1. **Build** — `pnpm build` ou deixar `pnpm dev` rodando.
2. **Lançar com CDP** — App deve ser iniciado com `--remote-debugging-port=9222` (ex.: `./dist/mac/App.app/Contents/MacOS/App --remote-debugging-port=9222` ou via script).
3. **Conectar** — `agent-browser connect 9222` (ou `npx agent-browser connect 9222`).
4. **Snapshot** — `agent-browser snapshot -i` para listar elementos interativos.
5. **Inspeção** — Verificar elementos, texto e estados esperados via snapshot/`agent-browser get text` e interações necessárias (click/fill) sem screenshot no fluxo diário.
6. **Dark mode** — Usar `--color-scheme dark` ou `AGENT_BROWSER_COLOR_SCHEME=dark` para preservar tema escuro.

O agente pode assim validar se o que foi implementado está correto na tela (layout, botões, texto) por inspeção. Captura de imagem deve ficar para comandos específicos de teste com evidência visual.

## Documentação do projeto

- **.agents/** — Planos (`plans/`), test-registry (`test-registry.md`), inspirações (`inspirations/`).
- **.cursor/rules/** — Regras de contexto do projeto.
- **.cursor/commands/** — Definições dos comandos.
- **.cursor/skills/** — Skills (plan-feature, inspiration-research, electron, full-stack-electron).
